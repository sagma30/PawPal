# Frontend Environment Variables
# VITE_ prefix required for Vite to expose vars to client-side code

# Backend API base URL (change for production deployment)
VITE_API_URL=http://localhost:3001/api/v1
